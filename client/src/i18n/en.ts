const en = {
  common: {
    ok: "Ok",
    cancel: "Cancel",
    close: "Close",
    loading: "Loading...",
    error: "Error",
    retry: "Retry",
    noResults: "No Results",
    save: "Save"
  },

  settings: {
    toggle: "Toggle #{name}"
  }
}

export default en;

export type Translations = typeof en;
